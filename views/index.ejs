<%- include("header", {title: title}); %>

<body>
<% for (const row of res) { %>
	<div class="match">
		<a href="watch?v=<%= row.id %>">
			<img src="/images/team-logos/<%= row.teams.home.abbr%>.svg" alt="home"/>
			<img src="/images/team-logos/<%= row.teams.away.abbr%>.svg" alt="away"/>
			<h1><%= row.teams.home.name %> - <%= row.teams.away.name %></h1>

			<div class="overlay">
				<h2><%= row.date %></h2>
				<h2><%= row.teams.home.abbr %> - <%= row.teams.away.abbr %></h2>
			</div>
		</a>
	</div>
<% } %>


	<%- include("footer"); %>
</body>

